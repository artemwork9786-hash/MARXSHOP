Topic: z-index fixes, fullscreen transition animation, local dev setup

# Session checkpoint
_Generated by checkpoint writer; structure preserved across updates. Edit only content under italic instructions._

## §1 Active intent
_User's most recent explicit request, verbatim block-quoted from the conversation. The verbatim quote is the ground truth for what to do — do not paraphrase._

> "давай сделаем плавный эффект затухания при открытии и закрытии режима на весь экран (как на ютубе)"
> "этот блюр нужно сделать темнее и сильнее, можно так сделать?"
> "МАКСИМАЛЬНО ПРОСТОЙ ЛОКАЛЬНЫЙ ТЕСТ MINI APP"
> "не работает, предумай новый простой план как протестировать наш проект, хотя бы с компьютера"

## §2 Next concrete action
_Following is the specific fix needed:_

User's latest request was about local testing. The local dev setup (`npm run local`) has been created. User reported it "не работает" and asked for a simpler plan. The agent suggested opening `http://localhost:5173/MARXSHOP/` directly in browser (no Telegram needed for UI testing). **Next step: verify the user can successfully test locally, or troubleshoot if it still fails.**

## §3 Directives (this session)
_Session-specific working style only. Project rules in MEMORY.md — do not duplicate._

- Никаких объяснений в выводе — строгое выполнение (from MEMORY.md Rules)
- User is frustrated with repeated failures — prioritize getting it RIGHT on first attempt
- Do NOT run long-running processes (node server, vite dev) directly in bash tool — they block indefinitely. Use detached spawn scripts instead.
- Do NOT use `start /b` from bash tool on Windows — it hangs. Use the `npm run local` script.

## §4 Task tree
_Hierarchical view of tasks with current focus marked. Pulled from task tool's DB._

(none — no tasks registered in task tool)

## §5 Current work
_Description of what was being done immediately before this checkpoint._

Three major workstreams completed this session:

**1. Extra info overlay z-index fix chain** — The overlay's `z-50` covered the Header (`z-50`) and BottomNav (`z-50`). Multiple attempts:
- `z-30` → video controls showed through
- `z-[45]` → price bar (`relative z-50`) still showed through
- Adding `z-0` to card for stacking context → broke fullscreen (video trapped inside)
- Final: `z-[55]` on overlay + removed `z-0` from card

But `z-[55]` broke fullscreen because `@container` on the card creates a stacking context, trapping video's `z-index: 9999`. Fixed by conditionally removing `@container` class when fullscreen is active (via `onFullscreenChange` callback from GlassPlayer → AccountCard).

**2. Fullscreen transition animation** — Added smooth fade-to-black (then blur) curtain effect:
- `isTransitioning` state + `dimOpacity` state with CSS transitions
- `setup-local.cjs` was created as a detached process launcher
- Flow: fade-in curtain (300ms) → switch fullscreen → fade-out curtain (300ms)
- User requested blur instead of solid black → `backdrop-filter: blur(40px) saturate(160%)` with `rgba(0,0,0,0.92)`

**3. Local dev testing** — Created `scripts/setup-local.cjs` (mirrors `setup-tunnel.cjs` pattern). Starts Express server + Vite dev server both detached, shows URL, exits. Added `npm run local` command. User reported it doesn't work — next agent should troubleshoot.

## §6 Files and code sections
_Files actively being read or modified._

- `src/components/AccountCard.jsx` — Main file. GlassPlayer + AccountCard. Key changes:
  - GlassPlayer: added `onFullscreenChange` prop, `isTransitioning` + `dimOpacity` states, fade curtain logic in `toggleFullscreen`
  - AccountCard: added `isCardFullscreen` state, conditional `@container` removal, dimming overlay div between card and fullscreen portal
  - Extra info overlay: `z-[55]`
- `scripts/setup-local.cjs` — New file. Local dev launcher. Kills old processes on ports 5000/5173, starts Express + Vite detached, prints URL, exits.
- `package.json` — Added `"local": "node scripts/setup-local.cjs"`, changed `"dev"` to `"vite --host"`
- `.mimocode/plans/project-launch.md` — Added "Локальная разработка" section

## §7 Discovered knowledge (cross-task)
_Cross-task session findings. Candidates for MEMORY.md promotion._

- **`@container` Tailwind creates CSS `container-type: inline-size` which creates a stacking context** — All child z-indexes are trapped inside the card's context. Video's `z-index: 9999` cannot compete with Header/Footer at document level. Fix: conditionally remove `@container` class during fullscreen.
- **Long-running processes hang the bash tool** — `node server.js` and `npx vite` never terminate, so bash waits forever. Must use detached spawn scripts (`setup-local.cjs` pattern) that launch processes and exit.
- **Bash `&` and `start /b` are unreliable in the bash tool** — Both can still cause hangs or silent failures. Dedicated Node.js scripts with `detached: true` + `unref()` are the only reliable pattern on Windows.

### Discovered
(none)

### Dead ends
(none)

## §8 Errors and fixes
_Errors encountered this session and how they were resolved._

- **Overlay z-50 covered Header/BottomNav** — Both Header and BottomNav use `z-50`. Overlay `absolute inset-0 z-50` competed at same level, won by DOM order. Fixed by raising overlay to `z-[55]`.
- **Price bar showed through z-[45] overlay** — Price bar div has `relative z-50` (line 579). `z-[45] < z-50`. Fixed by raising overlay to `z-[55]`.
- **`z-0` on card broke fullscreen** — Added `z-0` to create stacking context so overlay wouldn't affect header/footer. But this also trapped the video's `z-index: 9999`, making fullscreen invisible behind other cards/header. Fixed by removing `z-0` and using `z-[55]` instead.
- **`@container` trapped video z-index in fullscreen** — `@container` (Tailwind) creates `container-type: inline-size` = stacking context. Video's `z-9999` isolated inside card. Other cards and header/footer rendered on top. Fixed by conditionally removing `@container` class when `isCardFullscreen` is true, using `onFullscreenChange` callback from GlassPlayer.
- **Bash tool hangs on long-running processes** — `node server/server.js` and `npx vite --host` never exit. Bash tool waits for process completion indefinitely. Fixed by creating `scripts/setup-local.cjs` with `detached: true` + `unref()` pattern.
- **Dimming overlay opacity logic was inverted** — First attempt used `isFullscreen ? 1 : 0` opacity, but needed separate `dimOpacity` state that fades in → switch → fades out. Fixed with dedicated state and setTimeout chain.

## §9 Live resources
_Runtime state: branch, uncommitted files, running processes._

- Git repo: `E:\Projects\MARX SHOP`
- Last deploy: successful after fullscreen transition animation
- Running processes: Express server on port 5000, Vite dev server on port 5173 (may have been killed during testing)

## §10 Design decisions and discussion outcomes
_Decisions reached through discussion. Promote to MEMORY.md when durable._

- **Dimming overlay uses blur, not solid black** — User explicitly requested "наш уже привычный блюр" then "этот блюр нужно сделать темнее и сильнее". Final: `backdrop-filter: blur(40px) saturate(160%)`, `backgroundColor: rgba(0,0,0,0.92)`, 300ms ease-in-out transitions.
- **Fullscreen transition is a "curtain" pattern** — Black/blur overlay covers entire screen → switch happens at peak opacity → overlay fades out. Total transition ~600ms (300ms fade-in + 300ms fade-out).
- **Local testing without Telegram** — User can test UI by opening `http://localhost:5173/MARXSHOP/` directly in browser. API calls go to `localhost:5000`. Telegram-specific features (payments, back button) won't work, but all UI is testable.
- **Overlay z-index must be above card internals but below app shell** — `z-[55]` on overlay is above price bar (`z-50`) and card controls (`z-40`), but Header/BottomNav at document-level `z-50` should be above... However since card has no explicit z-index, overlay competes at document level too. Currently `z-[55] > z-50` means overlay IS above header/footer — this is acceptable because the overlay is only shown inside the card and `overflow-hidden` on card clips it visually.

## §11 Open notes
_Writer-curated catch-all. May be empty._

- User reported local testing "не работает" — unclear if the issue was `npm run local` not starting, or Telegram not opening localhost links, or something else. The agent suggested browser-only testing as alternative. Needs follow-up.

### Execution context
(none)

### Live resources
- Git repo: `E:\Projects\MARX SHOP`

### Session metadata
- Session ID: ses_08b093a30ffehNUniPtDIeR3G0
- Project: MARX SHOP
- Task: z-index fixes + fullscreen transition animation + local dev setup
